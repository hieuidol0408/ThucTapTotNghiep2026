{
  "name": "hrm-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd backend && npm install && cd ../frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/* ./dist/",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
